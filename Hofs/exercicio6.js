const produtos = require('./produtos');

// complete a função para que ela receba o id como parâmetro e retorne
// o item que corresponde aquele id, se não encontrar retornar uma mensagem dizendo "id não encontrado"
function verifyId() {

}

module.exports = verifyId;
