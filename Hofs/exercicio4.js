const produtos = require('./produtos');

// complete a função para retornar todos os produtos que tenham a diferença entre o preço máximo e o preço mínimo maior que 100
function differenceBetweenMaxAndMinPrice() {

} 

module.exports = differenceBetweenMaxAndMinPrice;
